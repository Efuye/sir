import { media } from "./values";
import errors from "./errors";

/**
 * This value is a container for all static resources that
 * are either generated or defined.
 *
 * @name R
 * @type { Object }
 * @readonly
 */
export const R: any = { values: { media }, errors };
